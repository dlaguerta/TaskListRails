<h1> Alyssa-Diane's List of Tasks </h1>

<ul>
<% @list_of_tasks.each do |task|  %>
  <li>
  <%= link_to("#{task.name}", task_path(task.id)) %>
  <br><%= task.task_status %>

  <br><%= link_to "Edit Task", task_edit_path(task.id) %>
  <br><%= link_to "Delete task", task_path(task.id),
                              method: :delete,
                              data: { confirm: 'Are you sure you want to delete this task?'} %>

  </li>
  <br>
<% end %>
</ul>

<br><%= link_to "Add a new task!", tasks_new_path %>
